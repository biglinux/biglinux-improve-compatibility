#!/bin/sh

if [[ $XDG_SESSION_TYPE == "wayland" ]] && type kdotool &> /dev/null ; then
    kdotool windowactivate $(kdotool search --name 'visual studio code - insiders' | head -n1)
    /usr/bin/code-insiders ${1+"$@"}
else
    /usr/bin/code-insiders ${1+"$@"}
fi
